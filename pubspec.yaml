name: widgets
description: widget library
version: 1.2.1
homepage: https://github.com/flutter-packagist/widgets

environment:
  sdk: '>=2.19.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  cached_network_image: ^3.3.1   # 网络图片加载
  easy_refresh: ^3.3.5+1         # 下拉刷新，上拉加载
  dio: ^5.4.3+1                  # 网络请求
  network:
    git:
      url: https://github.com/flutter-packagist/network.git
      ref: 1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
